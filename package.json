{
  "name": "generic-encrypted-sync-hub",
  "version": "1.0.0",
  "description": "Generic Encrypted Sync Hub (GESH) HTTP service",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
